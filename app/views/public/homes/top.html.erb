<div class="container my-5">
  <div class="row">
    
  <div class="col-md-3">
  <div class="p-3 border">
    <h5>ジャンル検索</h5>
    <ul class="list-group list-group-flush">
      <li class="list-group-item"><%= link_to "全商品", items_path %></li>
      
      <% @genres.each do |genre| %>
        <li class="list-group-item">
          <%= link_to genre.name, items_path(genre_id: genre.id) %>
        </li>
      <% end %>
      </ul>
  </div>
</div>

    <div class="col-md-9">
      
      <div class="p-4 mb-5 text-white" 
           style="background: url(<%= asset_path('about.jpg') %>) no-repeat center center; 
                  background-size: cover; 
                  border-radius: 5px;">
        
        <div class="p-3 bg-dark bg-opacity-50" style="text-align: center; border-radius: 3px;">
          <h2 class="text-white">ようこそ、ながのCAKEへ！</h2>
          <p class="mb-0">このサイトは、ケーキ販売のECサイトになります。</p>
          <p class="mb-0">会員でない方も商品の閲覧はできますが、<br>購入には会員登録が必要になります。</p>
        </div>
      </div>
      
      <h3 class="mb-4">新着商品</h3>
      <div class="row">
        <% @new_items.each do |item| %>  <div class="col-md-3 mb-4 text-center">
            
            <%= link_to item_path(item) do %> <div class="border d-flex justify-content-center align-items-center" style="height: 150px; overflow: hidden;">
                <%= image_tag item.get_item_image(150, 150), size: "150x150" %>
              </div>
            <% end %>
            
            <p class="mt-2 mb-0">
              <%= link_to item.name, item_path(item), class: "text-dark text-decoration-none" %>
            </p>
            <p>
              ¥<%= item.tax_included_price.to_s(:delimited) %>
            </p>
          </div>
        <% end %>
      </div>

      <div class="text-right">
        <%= link_to "全ての商品を見る >", items_path %>
      </div>
    </div>
  </div>
</div>