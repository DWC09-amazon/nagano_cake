<h1>商品詳細</h1>

<p>商品ID: <%= @item.id %></p>
<% if @item.item_image.attached? %>
  <%= image_tag @item.item_image, size: "200x200" %>
<% else %>
  <p>画像はありません</p>
<% end %>

<p>商品名: <%= @item.name %></p>
<p>商品説明: <%= @item.introduction %></p>
<p>ジャンル: <%= @item.genre.name %></p>
<p>税抜価格: <%= @item.price.to_s(:delimited) %> 円</p>
<p>税込価格: <%= @item.tax_included_price.to_s(:delimited) %> 円</p>
<p>販売ステータス:
  <% if @item.is_active? %>
    <span class="text-success">販売中</span>
  <% else %>
    <span class="text-secondary">販売停止中</span>
  <% end %>
</p>

<%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-info" %>
<%= link_to "一覧に戻る", admin_items_path, class: "btn btn-secondary" %>